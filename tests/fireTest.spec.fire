import * as inspect from 'inspect.js'
describe('FireTest', () =>
  it ('loads tests from a .fire file', () =>
    inspect('foo').isString()
  )

  it ('str is equal', () =>
    inspect('foo').isEql('foo')
  )
)
